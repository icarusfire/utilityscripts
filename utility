avg_over_time(
  (
    sum(rate(container_cpu_usage_seconds_total{cluster=~"$cluster",squad="OnePAM Platform",container!="",container!="POD"}[5m]))
    /
    sum(kube_pod_container_resource_requests{cluster=~"$cluster",squad="OnePAM Platform",resource="cpu"})
  )[1w:5m]
)
